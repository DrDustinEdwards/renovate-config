{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": [
    "The portfolio's shared Renovate policy. Ruling 33, dustinedwards/decisions-vol-16.md.",
    "Consumed by dustinedwards-info, dustinedwards-mcp and capsid-mcp, each of which carries a renovate.json that extends this file and nothing else, so the policy has ONE owner and a change here reaches every repo without three edits.",
    "Renovate was chosen over Dependabot on grouping, the dashboard issue, and automerge rules living in the same file. NOT covered by either, and deliberately left as quarterly human items: wrangler's compatibility_date, which is a date rather than a version, and Node, which is disabled outright in the last packageRule because engines.node and .nvmrc have to move in the same hand-written commit.",
    "Ruling 36, 2026-09-07: automerge ON for security fixes, the weekly minor and patch group, pins, and lockfile maintenance, gated on CI. Majors stay human-merged from the dashboard. A repo with no CI never automerges, because ignoreTests is false and Renovate refuses to automerge without a passing check.",
    "Ruling 42, 2026-09-08: the load-bearing six stopped being one refusal and became a floor plus four grants, because 'this package is load-bearing' and 'this UPDATE is risky' are different claims and the six were being judged on the first. The framework and the Cloudflare tooling now automerge on patch and minor, better-auth and wrangler on patch alone, and katex on nothing. All of them stay ungrouped at every type, which is the half of the old rule that was always right.",
    "Ruling pending, 2026-09-15: rebaseWhen is \"conflicted\" and prHourlyLimit is stated. Renovate was 613 of the September cycle 1,800 Actions minutes, 602 of them in dustinedwards-info, and the cause is rebasing rather than opening: single branches re-ran CI 20 to 37 times each (renovate/react-router-8.x 33 runs and 124 minutes, renovate/react-router-dev-8.x 37 runs, renovate/remark 22 runs and 76 minutes). config:recommended leaves rebaseWhen at \"auto\", which rebases every open branch each time the base moves, and each rebase costs a push run and a pull_request run. \"conflicted\" rebases only when a branch will not merge. No consuming repo has branch protection (the free plan refuses it, HTTP 403), so no required check is strict and a stale branch cannot stall automerge, which is the one condition that would make this setting cost more than it saves."
  ],
  "extends": ["config:recommended"],

  "timezone": "America/Chicago",
  "schedule": ["before 6am on monday"],

  "rangeStrategy": "pin",

  "dependencyDashboard": true,
  "labels": ["dependencies"],
  "prConcurrentLimit": 3,
  "prHourlyLimit": 2,
  "rebaseWhen": "conflicted",
  "postUpdateOptions": ["npmDedupe"],

  "automerge": false,
  "automergeType": "pr",
  "platformAutomerge": true,
  "ignoreTests": false,

  "lockFileMaintenance": {
    "description": "Monthly. Refreshes transitive pins that no direct dependency bump would move.",
    "enabled": true,
    "schedule": ["before 6am on the first day of the month"],
    "automerge": true
  },

  "vulnerabilityAlerts": {
    "description": [
      "Security fixes do not wait for Monday and do not ride in the weekly group.",
      "An empty schedule is what lets one open the moment it is found: the top-level schedule above would otherwise hold it for up to a week. minimumReleaseAge is null rather than a duration for the same reason, which is Renovate's documented way of saying no waiting period at all."
    ],
    "enabled": true,
    "schedule": [],
    "groupName": null,
    "minimumReleaseAge": null,
    "labels": ["dependencies", "security"],
    "automerge": true
  },

  "packageRules": [
    {
      "description": "ONE PR A WEEK for everything routine. A pile of single-package PRs is a pile nobody reads, and patch and minor are where the low-risk volume lives.",
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "weekly minor and patch",
      "automerge": true
    },
    {
      "description": "pinning a range to the installed version changes no bytes",
      "matchUpdateTypes": ["pin"],
      "automerge": true
    },
    {
      "description": "Majors get their own diff. A breaking change read next to nine unrelated bumps is a breaking change nobody read.",
      "matchUpdateTypes": ["major"],
      "groupName": null,
      "automerge": false
    },
    {
      "description": [
        "THE LOAD-BEARING SET: ungrouped at EVERY update type, and no automerge unless one of the four rules below grants it. Ruling 42 replaced the single blanket refusal this used to be, because refusing every type of every one of these treated a wrangler patch and a react-router major as the same risk, and they are not.",
        "This rule is the FLOOR the four below carve out of, and it is a separate rule rather than part of them for a reason Renovate's semantics force: automerge is one boolean per rule, so a policy that automerges patch and refuses minor cannot be written as one rule. Setting the floor here and granting per type below is what keeps each grant readable as the ruling wrote it.",
        "Membership is unchanged. Each one can break the site on its own: wrangler is the platform CLI and its compatibility surface, better-auth is the login, react-router and @react-router/* are the framework the whole app is written against, @cloudflare/* is the test and build plumbing against the real runtime, and katex is pinned to the exact version the generated stylesheet was derived from."
      ],
      "matchPackageNames": [
        "wrangler",
        "better-auth",
        "react-router",
        "katex",
        "@react-router/**",
        "@cloudflare/**"
      ],
      "groupName": null,
      "automerge": false
    },
    {
      "description": "The framework and the Cloudflare tooling automerge on patch and minor, and never on major. React Router 8 breaks only in majors, behind future flags, so a minor cannot change behaviour the app has not opted into; the Cloudflare plugins and types are build and test plumbing that CI exercises in full, so a green run is the whole claim. Majors fall through to the floor above and stay human-merged.",
      "matchPackageNames": [
        "react-router",
        "@react-router/**",
        "@cloudflare/vite-plugin",
        "@cloudflare/vitest-plugin",
        "@cloudflare/workers-types"
      ],
      "matchUpdateTypes": ["minor", "patch"],
      "automerge": true
    },
    {
      "description": "better-auth automerges on patch alone. Its 1.x minors carry breaking changes by the maintainers' own account, so a minor here is a major everywhere else and gets read. Security fixes ship as patches, which is the half that must not wait for a human.",
      "matchPackageNames": ["better-auth"],
      "matchUpdateTypes": ["patch"],
      "automerge": true
    },
    {
      "description": "wrangler automerges on patch alone. Its minors swap the local runtime underneath everything (4.117 moved to miniflare 5), and wrangler is what RUNS the deploy, which is the one path CI never exercises: a green CI run says nothing about whether the deploy still works. A patch is the largest bump that claim survives.",
      "matchPackageNames": ["wrangler"],
      "matchUpdateTypes": ["patch"],
      "automerge": true
    },
    {
      "description": "katex never automerges, at any update type, and is held below 0.17.0. The floor above already refuses it; this rule states it rather than leaving it implied, because the reason is specific and outlives the floor: app/styles/katex.generated.css and the twenty woff2 faces under app/fonts/katex/ are committed and byte-compared by check:content against a fresh derivation from the installed package, so a bump that does not run build:katex in the same PR is red by construction. The version ceiling is rehype-katex 7's peer range.",
      "matchPackageNames": ["katex"],
      "automerge": false,
      "allowedVersions": "<0.17.0"
    },
    {
      "description": [
        "image-size arrives ALONE and is never automerged, at any update type. Ruled 2026-09-15.",
        "The grounds are a specific episode rather than a general caution. Two high advisories against 2.0.2 (GHSA-w3rx-r6r6-pgpr and GHSA-5p2g-fcmc-qvqq, CVE-2025-71330 and CVE-2025-71329, infinite loops in the ICNS, JXL and HEIF parsers) could not be claimed by the vulnerabilityAlerts path, because NEITHER advisory database records a fixed version: GitHub carries no first_patched_version and OSV carries last_affected 2.0.2 with no fixed event. So the remediation had no target, and the fix instead fell through to the routine path, where it was bundled into the nineteen-package weekly group with automerge true and was six days from merging itself before 6am on a Monday with nobody reading it.",
        "A security fix that arrives disguised as routine volume is the one bump that must be read, and grouping is what hides it. groupName null unbundles it; automerge false is the half that matters, because an ungrouped PR that merges itself before anyone opens it is the same outcome in a smaller diff.",
        "There is a second, durable reason that outlives these two CVEs. The project moved off GitHub to codeberg.org mid-advisory: the GitHub repo's last push is 2026-06-03, its newest release is v2.0.2, the pull request both advisories cite now 404s and survives only in a web.archive.org snapshot, and 2.0.3 and 2.0.4 were published to npm on 2026-09-14 forty-one minutes apart with no build provenance and no GitHub tags. The publisher is the same sole maintainer account as 2.0.2, and the parser guards are present in the published bytes, both verified by hand. That is enough to take the bump deliberately and not enough to take it in a bundle at 6am.",
        "This rule is a no-op in dustinedwards-mcp and capsid-mcp: neither depends on image-size, measured 2026-09-15. It lives here anyway, because the policy has one owner (ruling 33) and a rule that fires in one repo is not a reason to fork the file."
      ],
      "matchPackageNames": ["image-size"],
      "groupName": null,
      "automerge": false
    },
    {
      "matchPackageNames": ["node"],
      "enabled": false,
      "description": "Node moves in the quarterly dependency session with compatibility_date, never by bot. check:invariants 16 requires engines.node to equal .nvmrc exactly; Renovate pinned engines to the newest Node while .nvmrc stayed on 24 (PR #19, 2026-09-07)."
    }
  ]
}
